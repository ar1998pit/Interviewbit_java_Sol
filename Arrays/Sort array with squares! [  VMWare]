public class Solution {
    public ArrayList<Integer> solve(ArrayList<Integer> A) 
        {
            
            for(int i=0;i<A.size();i++){
                int n = A.get(i);           
                A.set(i,(n*n));
               
            }
            Collections.sort(A);
            return A;
        }
    }
    
